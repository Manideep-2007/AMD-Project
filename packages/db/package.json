{
  "name": "@nexusops/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:prod": "prisma migrate deploy",
    "build": "prisma generate",
    "lint": "echo 'No linting for DB package'",
    "studio": "prisma studio",
    "seed": "tsx src/seed.ts",
    "clean": "rimraf .prisma node_modules"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
